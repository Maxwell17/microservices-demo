server:
  port: 8084
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: users
          uri: lb://city-service
          predicates:
            - Path=/users/**
        - id: cities
          uri: lb://city-service
          predicates:
            - Path=/cities/**